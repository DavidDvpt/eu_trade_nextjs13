type SelectType = {
  value: string;
  label: string | import('react').ReactElement;
};
type SelectTypes = SelectType[];

interface IChildren {
  children: import('react').ReactNode;
}

interface IId {
  id: string;
}
